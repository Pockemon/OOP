#include<bits/stdc++.h>

using namespace std;

int main()

{
    for(int i=1;i<10;i++)
    {
        cout.width(10);

        cout.fill('*');

        cout.precision(6);

        cout<<sqrt(i)<<endl;
    }
}
